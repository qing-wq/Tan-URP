server:
  port: 8080
  servlet:
    session:
      timeout: 5m # 设置session的有效期为五分钟

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/${database.name}?useUnicode=true&characterEncoding=UTF-8&useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: root
#  redis:
#    host: localhost
#    port: 6379
#  data:
#    mongodb:
#      uri: mongodb://root:root@localhost:27017/urp?authSource=admin&authMechanism=SCRAM-SHA-1
#      field-naming-strategy: org.springframework.data.mapping.model.SnakeCaseFieldNamingStrategy

database:
  name: urp